# Improvements
- Remove `Edicion` from dashboard on profile but keep this wor on edit mode.
- On dashboard page show if an image is hidden from public profile, as we do in edit mode.
- On dashboard page add a label to represent the gallery images.
- On daskboard page and edit mode add a background image to each section header. The backgroun can be a blue with degradation as header.