On `Buscar` when we do click in `Mostrar mas` we already know how many items are we going to load, so I think a good approach is to add just amount of scheleton that we expect to retrieve in the next load. Another issue I saw is that you hide the already retrived cards, so the idea is add add the end this scheleton and the incorporate the result