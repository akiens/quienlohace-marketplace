

# Asistente creador de perfil
- En el paso `Especialidades` necesito que cuando se elija una especialidad que automaticamente aparezcan como primera sugerencia en el select las especialidades relacionadas con el rubro de la especialidad seleccionada. Si se selecciona otra especialidad con un rubro diferente entonces hacemos lo mismo pero estas especialidades se listan despues de las del primer rubro.